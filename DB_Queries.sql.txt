Create Table role(
   ROLE_ID INT NOT NULL AUTO_INCREMENT,
   ROLE_NAME VARCHAR(15) NOT NULL,
   ACTIVE CHAR(1) DEFAULT 'Y', 
   CREATE_DATE DATE,
   UPDATE_DATE DATE,
   PRIMARY KEY ( ROLE_ID )
);

Create Table user(
   USER_ID INT NOT NULL AUTO_INCREMENT,
   FIRST_NAME VARCHAR(15) NOT NULL,
   LAST_NAME VARCHAR(15) NOT NULL,
   LOGIN_NAME VARCHAR(30) NOT NULL,
   PASSWORD VARCHAR(30) NOT NULL,
   ACTIVE CHAR(1) DEFAULT 'Y', 
   ROLE_ID INT NOT NULL,
   CREATE_DATE DATE,
   UPDATE_DATE DATE,
   PRIMARY KEY ( USER_ID )
);

Create Table audit(
   ID INT NOT NULL AUTO_INCREMENT,
   EVENT_NAME VARCHAR(30) NOT NULL,
   EVENT_ID VARCHAR(50) NOT NULL,
   LOGIN_NAME VARCHAR(30) NOT NULL,
   UPDATE_DATE DATE,
   NOTES VARCHAR(100),   
   PRIMARY KEY ( ID )
);

INSERT INTO user(FIRST_NAME,LAST_NAME,LOGIN_NAME,PASSWORD,ROLE_ID,CREATE_DATE,UPDATE_DATE) VALUES ('Rohit','Saxena','rohit.saxena','abc',1,'2017-11-17','2017-11-17');
INSERT INTO user(FIRST_NAME,LAST_NAME,LOGIN_NAME,PASSWORD,ROLE_ID,CREATE_DATE,UPDATE_DATE) VALUES ('Piyush','Vaishnav','ts.piyush','abc',2,'2017-10-17','2017-10-17');

CREATE USER 'fsegroup'@'%' IDENTIFIED BY 'fsegroup';
GRANT ALL PRIVILEGES ON library.* TO 'fsegroup'@'%';